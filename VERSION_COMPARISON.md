# 版本对比：旧版 vs 简化版

## 🔍 核心差异一览表

| 功能项 | 旧版（Web3 Dashboard） | 新版（Simplified） |
|-------|----------------------|-------------------|
| **每次 API 调用** | 2000+ units | **102 units** ✅ |
| **关键词模板** | 自动跑多个查询 | 六选一单次查询 ✅ |
| **search.list 调用** | 12-18 次 | **1 次** ✅ |
| **结果数量** | 不限（全部显示） | Top 5 ✅ |
| **订阅数过滤** | 5k+ | **100k+** ✅ |
| **缓存** | 7 天 TTL（3 层缓存） | **24h TTL**（单层内存） ✅ |
| **Debug 模式** | 主界面切换 | 已移除 ✅ |
| **Test 模式** | 主界面切换 | 已移除 ✅ |
| **Quota Status** | 大面积卡片 | 小型 badge ✅ |
| **X 平台支持** | 未配置 | 未配置（显示提示） ✅ |

---

## 📊 API 配额对比

### 旧版本（多查询模式）

```plaintext
竞品: WEEX
关键词: 自动生成 12-18 条查询（ref, referral, invite, promo code...）

API 调用:
├─ search.list × 12 = 1200 units
├─ videos.list × 多次 = ~50 units
├─ channels.list × 多次 = ~50 units
└─ 总计: ~1300-2000 units

配额消耗: 每天 10,000 units → 只能跑 5-7 次
```

### 新版本（单查询模式）

```plaintext
竞品: WEEX
关键词: 用户选择 1 个模板（例如"竞品+联盟"）

API 调用:
├─ search.list × 1 = 100 units ✅
├─ videos.list × 1 = 1 unit ✅
├─ channels.list × 1 = 1 unit ✅
└─ 总计: 102 units ✅

配额消耗: 每天 10,000 units → 可以跑 98 次 🎉
```

**节省率: 95%+**

---

## 🎯 UI 对比

### 旧版本（Web3 Dashboard）

```
┌──────────────────────────────────────────────────────────┐
│  Header + Platform Badge                                 │
├──────────────────────────────────────────────────────────┤
│  ┌────────────┐  ┌────────────────────────────────────┐ │
│  │  Config    │  │  Quota Status (大卡片)             │ │
│  │            │  │  - search.list calls: 12           │ │
│  │  平台 Tabs │  │  - videos.list calls: 8            │ │
│  │  Debug 🔘  │  │  - cache hits: 5                   │ │
│  │  Test  🔘  │  │  - estimated cost: 1500 units      │ │
│  │  竞品 ▼    │  ├────────────────────────────────────┤ │
│  │  Run 按钮  │  │  Debug Stats (折叠面板)            │ │
│  └────────────┘  │  - After search: 150               │ │
│                  │  - After filter: 80                 │ │
│                  │  - After context filter: 50         │ │
│                  └────────────────────────────────────┘ │
│                  ┌────────────────────────────────────┐ │
│                  │  Results (全部频道 + 完整表格)      │ │
│                  │  - 显示所有命中频道（不限数量）     │ │
│                  │  - 复杂打分维度（多列）             │ │
│                  └────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

### 新版本（Simplified）

```
┌──────────────────────────────────────────────────────────┐
│  🎯 BD KOL Analytics        [⚡ Cache Hit] [🕐 Reset]   │
├──────────────────────────────────────────────────────────┤
│  ┌────────────┐  ┌────────────────────────────────────┐ │
│  │  Config    │  │  Top 5 Channels ✨                 │ │
│  │            │  │                                     │ │
│  │  竞品 ▼    │  │  #1 Channel (👥 500K) 🎯 85       │ │
│  │  平台 ▼    │  │     💬 Evidence: "partnership..."  │ │
│  │            │  │                                     │ │
│  │  🔑 模板   │  │  #2 Channel (👥 300K) 🎯 72       │ │
│  │  (六选一)  │  │     💬 Evidence: "futures..."      │ │
│  │            │  │                                     │ │
│  │  [ ] 模板1 │  │  #3 Channel (👥 250K) 🎯 68       │ │
│  │  [✓] 模板2 │  │  #4 Channel (👥 200K) 🎯 65       │ │
│  │  [ ] 模板3 │  │  #5 Channel (👥 150K) 🎯 60       │ │
│  │  [ ] ...   │  │                                     │ │
│  │            │  │  (仅显示订阅 >= 100k)               │ │
│  │  🚀 Run    │  └────────────────────────────────────┘ │
│  └────────────┘                                         │
└──────────────────────────────────────────────────────────┘
```

---

## 🚀 关键词模板对比

### 旧版本

```javascript
// 自动生成 12-18 条查询
[
  "WEEX ref",
  "WEEX referral",
  "WEEX invite",
  "WEEX exchange ref",
  "WEEX exchange referral",
  "WEEX exchange invite",
  "WEEX trading ref",
  "WEEX trading referral",
  "WEEX trading invite",
  "WEEX crypto ref",
  "WEEX crypto referral",
  "WEEX crypto invite",
  "WEEX review ref",
  "WEEX review referral",
  "WEEX review invite",
  "WEEX partnership",
  "WEEX sponsored",
  "WEEX referral",
  "WEEX promo code",
  // ...
]
```

每个查询都要调用 search.list（100 units），总计 1200-1800 units。

### 新版本

```javascript
// 用户选择 1 个模板（六选一）
[
  { id: 'contract_rebate', query: 'perps fee rebate' },
  { id: 'contract_partnership', query: 'futures partnership program' },
  { id: 'contract_code', query: 'crypto futures referral code' },
  { id: 'competitor_partnership', query: '{competitor} futures partnership' },
  { id: 'signals_vip', query: 'futures signals VIP join' },
  { id: 'tutorial_referral', query: 'tutorial perps referral link' },
]
```

只跑用户选择的 1 个模板，例如:
- 选择 `competitor_partnership` → 查询 "WEEX futures partnership"
- 只调用 1 次 search.list（100 units）

---

## 🎛️ 功能差异

| 功能 | 旧版 | 新版 |
|------|------|------|
| Debug Mode | ✅ 主界面开关 | ❌ 已移除 |
| Test Mode | ✅ 主界面开关 | ❌ 已移除 |
| 平台切换 | Tab 标签 | 下拉选择 |
| 竞品选择 | 下拉 | 下拉 |
| 关键词自定义 | 自动生成 | **六选一卡片** ✅ |
| 结果数量 | 不限 | **Top 5** ✅ |
| 订阅过滤 | 5k+ | **100k+** ✅ |
| Quota 显示 | 大卡片 + 详细统计 | **小 badge** ✅ |
| Debug Stats | 折叠面板 | 浏览器控制台 |
| CSV 导出 | ✅ | ❌（可后续添加） |
| 离线模式 | ✅ | ❌（可后续添加） |

---

## 📈 适用场景

### 旧版本适合:

- 需要大批量搜索多个关键词
- 需要详细的调试统计
- 需要导出 CSV 用于人工验收
- 配额充足（或使用缓存/离线模式）

### 新版本适合:

- **配额有限，需要精准控制每次调用**
- 只需要高质量频道（>= 100k 订阅）
- 聚焦单个关键词方向的深度分析
- 快速验证某个竞品在某个维度的表现

---

## 🔄 如何选择？

### 选择新版本（推荐）如果:

✅ 你的 YouTube API 配额有限（每天 10,000 units）  
✅ 你只关心高质量 KOL（>= 100k 订阅）  
✅ 你希望每次只测试一个关键词方向  
✅ 你希望界面简洁、无噪音  
✅ 你需要快速验证假设（102 units/次，可跑 98 次）

### 选择旧版本如果:

⚠️ 你有充足的 YouTube API 配额（或多个 API key）  
⚠️ 你需要全面覆盖多个关键词维度  
⚠️ 你需要详细的 Debug 统计和 CSV 导出  
⚠️ 你已经有大量缓存数据（7 天 TTL）

---

## 🎉 总结

| 维度 | 旧版 | 新版 |
|------|------|------|
| **配额效率** | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| **UI 简洁度** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **功能完整度** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **调试能力** | ⭐⭐⭐⭐⭐ | ⭐⭐ |
| **适合场景** | 全面分析 | **精准分析** ✅ |

**推荐**: 如果你的主要痛点是 **YouTube API 配额消耗过快**，新版本是最佳选择！

---

## 📦 文件清单

### 旧版本文件

- `app/page.tsx` → `app/page-web3-backup.tsx`（备份）
- `app/styles-web3.css` → `app/styles-web3-backup.css`（备份）
- `app/api/run-youtube/route.ts`（保留）

### 新版本文件

- `app/page-simplified.tsx` → `app/page.tsx`（新主页）
- `app/styles-simplified.css` → `app/styles.css`（新样式）
- `app/api/run-single-query/route.ts`（新 API）

---

## 🚀 立即切换

```bash
# 备份旧版本
cp app/page.tsx app/page-web3-backup.tsx
cp app/styles-web3.css app/styles-web3-backup.css

# 激活新版本
mv app/page-simplified.tsx app/page.tsx
mv app/styles-simplified.css app/styles.css

# 重启服务器
npm run dev
```

访问 `http://localhost:3001` 查看新版本！🎉
